/**
 * 1. create firebase project
 * 2. create web app
 * 3. npm i firebase
 * 4. save firebase config and export default app
 * 5. Build > Authentication> get started> enable sign in method
 * 6. create sign up and logun route
 */

/**
 * 1. create a context provider file
 * 2. create a context and set provider
 * 3. set the children props
 * 4. set context value
 * 5. set the provider
 */

/**
 * HOSTING
 * -------------
 * 1. one time per computer
 * 2.npm install -g firebase-tools
 * 3.firebase login
 *
 * for each project on time
 * 1. firebase init
 * 2. proccessed
 * 3. Hosting: configure files for firebase(up arrow and space bar select )
 * 4. existing project
 * 5. select the project
 * 6. which project as public directory use: vite er jonno dist/ npm er jonno build
 * 7. single page application: yes
 * 8. continuous deployment: no;
 *
 * for everytime
 *
 * 1. npm run build
 * 2. firebase deploy
 */
